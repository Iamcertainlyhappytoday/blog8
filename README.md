This is to be able to update blog entries from an interface. I want to know if it will work before finishing the project, if doing nodjs really can be done without hosting. Server mocking from a computer is one thing but hosting on a server without a package I suppose is what I mean. Except that package is like a reserved word for that now, IAAS, PAAS, it really is about being able to hang it up there while you are not there. Then it is are you the only author of page changes apart from the api to the database. There will be a lot of pages.
